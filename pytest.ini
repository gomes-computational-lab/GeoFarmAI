[pytest]
testpaths = tests
markers =
    integration: exercises multiple current pipeline stages and file outputs
    optional_scientific: requires an optional scientific dependency
    r: requires rpy2, R, and the configured R spatial packages
