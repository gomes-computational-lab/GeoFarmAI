# Synthetic example

The CSV files in `data/` are small deterministic synthetic samples created for documentation and tests. They are not copied from the research data in the source archive and must not be interpreted as agronomic observations.
